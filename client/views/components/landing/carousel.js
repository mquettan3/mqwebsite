Template.carousel.onRendered(function () {
//    This just needs to be called once per page.  Since the navbar will be on every page, the navbar is taking this resposibility
//    Modules.client.landing_dynamics_onRendered();
});

Template.carousel.destroyed = function () {
//    This just needs to be called once per page.  Since the navbar will be on every page, the navbar is taking this resposibility
//    Modules.client.landing_dynamics_destroyed();
};
